enum PasswordValidationResults { valid, tooShort, emptyPassword }

enum EmailValidationResults { valid, notValid, emptyEmail, emailStartWith }

enum PhoneValidationResults { valid, tooShort, notMatched, emptyPhone }
